export const assetTypes = ['Featured', 'KPI', 'Layouts', 'Storyboards']

export const initAssetNum = 10
export const showMoreNum = 8
export const trendingAssets = 4

export const COPY_LINK_PATH = 'AssetView'

// How long (in seconds) fetched data may be served from Next.js's cache
// before being revalidated in the background. Previously every request
// used `cache: 'no-store'`, so every page load did a live network round
// trip with no caching at all.
export const LIST_REVALIDATE_SECONDS = 60
export const DETAIL_REVALIDATE_SECONDS = 60
